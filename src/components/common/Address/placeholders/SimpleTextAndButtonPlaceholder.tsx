import React from 'react';

type ComponentProps = {
  handleOpenAddressPicker: () => void;
};

const SimpleTextAndButtonPlaceholder: React.FC<ComponentProps> = ({ handleOpenAddressPicker }) => {};

export default SimpleTextAndButtonPlaceholder;
